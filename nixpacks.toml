[phases.setup]
nixPkgs = ["python312", "nodejs_20", "gcc"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd wdk-service && npm install --omit=dev"
]

[start]
cmd = "bash start.sh"
